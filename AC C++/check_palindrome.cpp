//28-05-21 10.

//Character arrays - Check if a string is palindrome or not
//Time Complexity -

#include <iostream>

void checkPalindrome()
{
}

int main()
{
    int n;
    std::cin >> n;

    char arr[n];
    std::cout << sizeof(arr) / sizeof(arr[0]) << std::endl;
    std::cin >> arr;
    std::cout << sizeof(arr) / sizeof(arr[0]) << std::endl;
    std::cout << arr << std::endl;
    std::cout << sizeof(arr) / sizeof(arr[0]) << std::endl;

    return 0;
}